## Pending Work

- [ ] Restore clipboard copy/paste interactions within the grid (Ctrl/Cmd + C/V, context menu).
- [ ] Enable column and row header clicks to select entire column or row ranges.
- [ ] Keep toolbar and formula bar pinned to the top while scrolling the sheet body.
- [ ] Fetch and display signed-in user details (name, email) inside the header UI.
